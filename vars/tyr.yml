---
install_host_packages:
  - hunspell
  - vault

install_host_aur_packages:
  - amazon-ecr-credential-helper
  - jsonnet
  - libmysqlclient

install_asdf_plugins:
  - terraform

configuration_mpd_enabled: yes
