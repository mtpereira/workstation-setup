---
wallpaper_path: "{{ ansible_user_dir }}/.wallpaper.png"
xsession_commands:
  - xrdb -load "{{ ansible_user_dir }}/.Xresources"
  - feh --bg-fill "{{ wallpaper_path }}"
  - exec i3
pip_packages:
  - ansible
  - boto3
  - virtualenv
  - httpie
  - awscli

